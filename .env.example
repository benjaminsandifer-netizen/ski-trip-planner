# OpenWeatherMap API Key — get one free at https://openweathermap.org/api
VITE_OPENWEATHERMAP_API_KEY=

# Supabase — get these from your project's Settings > API page
# The app works without Supabase (falls back to localStorage), so these are optional.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
